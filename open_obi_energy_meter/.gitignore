.vscode/
.pio/
build.log
upload.log
*.log
# generated per-build by version.py (local, per-machine)
version.txt
include/fw_version.h
# local bridge address + web login for `pio run -t push` (see platformio.local.ini.example)
platformio.local.ini
# CI staging dir for release binaries (.github/workflows/build.yml)
dist/
PROJECT_NOTES.md
# LoRaWAN OTAA credentials (WP2) -- copy include/lorawan_secrets.h.example -> lorawan_secrets.h
include/lorawan_secrets.h
